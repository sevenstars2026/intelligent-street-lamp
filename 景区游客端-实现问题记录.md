# 景区游客端 - 实现问题记录

> 日期：2026-07-08  
> 用途：记录实现过程中发现的问题、处理结论和仍需二次确认的事项。

## 1. npm audit 依赖漏洞提示（已修复）

执行 `npm install multer @types/multer` 后，npm audit 提示：

- 3 个漏洞
- 2 个 moderate
- 1 个 high

处理结果：

- 漏洞来源不是新增的 `multer`，而是原有依赖：
  - `node-cron@3.0.3` 依赖的旧版 `uuid`
  - `nodemailer@6.9.7`
- 已采用非破坏性定向升级：
  - `node-cron` 升级到 `^4.6.0`
  - `nodemailer` 升级到 `^9.0.3`
- 已重新生成 `package-lock.json`
- 未执行 `npm audit fix --force`

验证结果：

- `npm audit --json`：0 个漏洞
- `npm run build`：通过

## 2. 接口测试记录（保留，不直接删库）

本轮为了验证真实 MySQL 写入链路，临时在 `3100` 端口启动后端并提交了一条游客故障上报测试数据：

- `alarmId`: 8
- `reportId`: 1
- `alarmType`: `fault_report`
- 状态：已通过 `/api/alarms/8/resolve` 标记为 `resolved`

处理结论：

- 该记录不影响活动告警列表。
- 当前不直接删除外部 MySQL 历史数据，避免误删真实业务记录。
- 如后续需要清理测试数据，应在数据库备份或明确授权后，按 `alarmId=8`、`reportId=1` 精确删除。
